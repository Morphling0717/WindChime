# Changelog

## 未发布 — 展示主题与排版设计（目标 0.8.0）

- 图片固定在文字下方，保持完整比例；仅正文、昵称和图片说明循环滚动。图片区占高可设为 20%–70%，默认 45%；多图通过编号对应文字说明。观众端去除固定品牌字样，主题名称只保留在私人界面。
- 直播工作台提供五类可独立置顶的私人磁贴，共享选信、审核和待播业务；关闭及跨话题操作保护未保存稿，旧窗口请求不能串箱。
- 下一封支持自定义全局热键及录制，配置失败保留旧设置；录制不会触发下一封，在线连接和状态版本仍由主进程及共享服务校验。
- 六种独立排版：经典信笺、文字双栏、横向条幅、弹幕侧栏、竖向信笺、居中短笺，均可搭配纯净、UliUli 夜航、Mia 星祷。竖向排版提供推荐画面尺寸，选择排版本身保留原尺寸。
- 增加边框、强调色、行高、字距、最大宽度及真实渲染器私下即时预览；旧话题数据缺省为纯净，外观调整不改变审核或播放状态。
- 共享外观契约和服务端验证扩展，capabilities 声明 `displayThemes`；旧站点可在新界面中预览，保存前明确提示升级。
- 自包含交互样板见 `examples/display-designs`，实现与验证记录见 `docs/DISPLAY-DESIGNS.md`。本节为设计实现，未替换 0.7.1 安装包或生产网站。
- 开发依赖 React、React DOM、react-test-renderer 固定为匹配的 19.2.8，修复原锁文件中 React DOM 19.2.5 导致的渲染版本冲突；公开 peer 兼容范围不变。

## 桌面 0.7.1 — 2026-09-13

- 修复宽窗口高度较小时，收件箱和直播工作台的话题下拉框被挤到侧栏可见区域外的问题；网站与话题选择统一置于主区顶部，所有宽度只有一个入口。
- 保留话题读取状态和旧密钥范围提示；切换继续使用主进程既有撤下流程，未保存稿需确认，取消保持原话题。
- 新增两页面五种窗口尺寸的可见性、遮挡、实际话题切换和未保存稿回归。共享库及网站继续使用 0.7.0，无服务端迁移。
- 本次验收见 `docs/DESKTOP-071-VALIDATION.md`。

## 0.7.0 — 2026-09-13

- 网页 `/mail` 增加共享连接密钥管理；`/mail/live` 仅兼容重定向，直播审阅和播出集中在桌面。
- 新密钥明确授予全站管理，现有及未来活动自动可见。旧令牌、配对和撤销关系原样保留为单话题权限，升级不自动扩权。
- 桌面补齐收件箱批量操作、CSV、话题管理、归档恢复、黑名单、二维码和海报；管理请求复用网站服务，独立展示窗口仍只有一个话题的只读权限。
- 违禁词匹配默认关闭，由站点控制授权在桌面设置切换；关闭时历史命中信正常显示，词库及历史标记保留。切换不改变人工播出批准。
- 当前验收与上线证据见 `docs/V070-VALIDATION.md`，旧报告只代表对应历史版本。

## 桌面 0.6.3 — 2026-09-13

- 使用用户确认的 `windchime_logo` 品牌资产，按用途替换应用图标、托盘、宽窄导航及安装动画，保留完整原始资产和索引。
- 小尺寸图标使用同源简化白色图形与品牌深色底板；安装画面使用原比例琉璃中英组合，不再引用早期生成图。
- 共享库仍为 0.6.1，网站与审核逻辑不变。资源、界面和打包验证见 `docs/BRAND-VALIDATION.md`。

## 桌面 0.6.2 — 2026-09-13

- 私人控制台改为液态玻璃视觉，新增侧栏导航、连接欢迎页、独立外观页及窄窗口布局；导航切换保留未保存稿。
- 一键隐藏与结束展示固定在顶栏。网站权限、审核规则及独立展示窗口维持原有接口。
- Windows 安装包加入同风格品牌动画，构建时从现有 Logo 生成，不增加运行依赖。
- 本次只升级桌面版本；共享库仍为 0.6.1，网站不变。实际视觉、回归和安装包核验见 `docs/GLASS-VALIDATION.md`。

## 0.6.1 — 2026-09-13，待发布

- 新增网页生成的桌面连接密钥，自生成起有效 30 天，可重复导入，也可由多台电脑共用；按话题授权，不改变审信和手动上屏规则。
- 撤销密钥会同时撤销使用该密钥的所有电脑连接及派生展示授权；需要单独管理时可分别生成密钥。原浏览器 PKCE 配对保留为备用流程。
- 桌面导入前验证网站能力与密钥授权，继续使用本机加密凭据和独立只读展示窗口。重启、连接切换和授权失效时保持空白。
- UliUli 与 Mia 固定升级到相同 0.6.1 包；网站需要完成实际部署才能启用新密钥流程。本轮不进行生产部署，不修改真实平台密钥或原数据库。
- 新增 `docs/CONNECTION-KEYS.md`；实际执行结果由 `docs/KEYS-VALIDATION.md` 单独记录，历史 0.6.0 验收报告保持原样。

## 0.6.0 — 2026-09-11

- 新增独立上屏稿、不可变批准快照、每话题完整有序批准列表；批准与手动上屏严格分离。
- 私有静态图片规范化上传、一次性投稿收据、主播替换图片；原信保留，内容变更必须重审。
- 设备配对与可撤销只读展示凭据，展示端只能读取当前快照及对应图片。
- 共享展示默认 1 秒轮询，桌面使用 500 ms；3 秒租期、版本冲突、重启和重连空白，删除、屏蔽、归档、撤销事务化撤下。
- 共用私人控制台、可替换展示组件、独立 Next 示例及 Windows Electron 应用。桌面默认直接连接站点，直播姬或 OBS 采集独立展示窗口；网页与桌面继续共享服务器数据和业务规则。
- B 站网关、H5 和平台生命周期适配保留为可选模块。本地桌面流程不依赖官方启动、平台密钥、项目 ID 或上架审核；不改动已有平台项目及私有配置。
- UliUli 升级至同一 0.6 包；Mia 从公开 0.3.1 接入迁移到共享业务。未部署到生产、未发布 npm；官方平台启动与上架不属于当前交付目标。
- 运行方式见 docs/LIVE.md，桌面打包见 apps/desktop/README.md。实际验证以各报告注明的执行日期和范围为准；旧平台报告作为历史记录保留，本次文档更新不代表新增测试通过。

## 0.5.0 — 待发布

- 新增 core/client/react/server/sqlite/next/media/ui 清晰导入入口；旧根组件导入保持兼容。
- 将两站共同的 SQLite 初始化、投稿、消息、审核、话题、归档、屏蔽、敏感词和限流集中维护。
- 提供完整 headless Hooks、CSV 和二维码/海报功能；默认组件复用公共逻辑。
- 修复公开备注/管理属性泄露、列表计数不一致、错误话题操作返回成功、切换请求竞争，以及归档、屏蔽、删除和限流的原子性。
- 旧表探测迁移保留数据、身份盐和默认话题；两个宿主的登录继续独立。
- 提供完整无 Tailwind 示例、登录适配说明、联调脚本、真实包检查及 CI。

迁移时请阅读 docs/MIGRATION.md。公开投稿结果改为最小成功对象；公共话题对象不再包含管理字段；普通列表计数不含待审核信件。未实际发布到 npm。

## 0.4.0

已有可选 React 组件、多话题 UI 和时间工具；后端由宿主实现。仓库内历史标签与文档保留，不重新发布旧版本。
