# 桌面 0.6.2 液态玻璃改版验收

执行日期：2026-09-13，Windows x64，Electron 44.3.0。共享库保持 0.6.1，本次未修改或部署 UliUli / Mia。使用临时配置和本机合成信件，未访问真实信箱。

## 实现范围

- 冰蓝与薄荷玻璃界面、侧栏、连接欢迎页、审阅和待播卡片、独立外观及连接页、760 像素窄窗口布局。
- 顶部固定“一键隐藏”和“结束展示”，使用既有主进程隐藏方法和共享服务端命令。导航不卸载审稿组件，未保存稿继续保留。
- 界面专属 CSS；网站组件样式、观众展示页、授权边界及后台审核规则不变。
- 安装器配套原 Logo 的 640×400 动画，36 帧，每帧 90 ms，循环 3.24 秒。生成器、Forge 配置及安装资源核验方法随代码交付。
- 玻璃效果是应用内部的模糊、透明叠层和反光。安装 GIF 不具备实时桌面模糊；Windows 系统安全与权限弹窗不换肤。

## 实际执行

命令工作目录为 `apps/desktop`。相同构建完成后，部分测试直接执行底层命令以避免重复生成资源。

| 命令 | 结果 |
| --- | --- |
| `npm test` | 构建与 37/37 主进程、授权、并发及清屏测试通过；最终源码另复跑 `node --test tests/*.test.cjs`，37/37 通过 |
| `../../node_modules/.bin/tsc --noEmit -p tsconfig.json` | 通过，无类型错误 |
| `node_modules/.bin/electron scripts/glass-preview.cjs` | 真实 Electron + 主进程 IPC + 临时模拟站点，11 张截图通过，控制台和页面错误均为 0 |
| `node_modules/.bin/electron scripts/smoke.cjs` | 19 项流程与权限回归通过，控制台错误为 0 |
| `npm run make` | Windows x64 安装包、ZIP、nupkg、RELEASES 及校验和全部生成，退出码 0 |
| `node scripts/verify-package.cjs <make 输出的临时目录>` | 通过，版本、运行文件、校验和及 Setup 内嵌动画一致；不执行安装器 |

最终视觉报告时间为 `2026-09-12T18:08:38.702Z`（北京时间 9 月 13 日）。输出：`apps/desktop/out/glass-preview/report.json` 和同目录截图。

视觉场景覆盖 1440×1000、1050×920、760×900。全部无横向溢出；隐藏按钮均通过视口边界及 DOM 命中检查，连接后的 9 张截图中结束展示同样可点击。原图和预览图加载相同的规范化测试图片字节。真实密码输入框和连接按钮完成连接，成功后密码框清空；导航往返、结束展示和隐藏后草稿保留，2 封批准队列不变，未审核信件保持未审核。模拟服务确实收到 `end`、`hide`，未收到 `show`、`approve`，没有自行打开展示窗口。

本机其他程序占用 `Ctrl+Shift+H`，应用在私人界面正确显示警告；未关闭其他程序或掩盖警告。按钮与托盘入口保留。本轮未穷举所有 Windows 缩放和辅助功能组合。

## 安全回归实测

`apps/desktop/out/smoke/report.json` 记录本轮原始值：

- 未审核及刚批准时为空白，手动上屏才显示审核稿，原文不进入输出；撤销撤下，刷新和恢复必须重新手动上屏。
- 控制与展示 preload 隔离，跨话题请求和展示端管理请求被拒绝；不访问 B 站或网关。
- 展示窗口完整遮挡 65,012 ms，期间仍收到 130 帧轮询更新。
- 模拟网页隐藏响应后 DOM 清空 **513 ms**；透明原生表面撤下 **343 ms**。
- 冻结 renderer 后看门狗清空原生表面 **2,589 ms**；恢复后的透明图像非透明像素数为 0。
- 显式断线清空 **612 ms**。这些是本机模拟站点单次实测，不是生产网络延迟保证。

## 包与验收边界

产物位于 `apps/desktop/out/installers`：`WindChime-Setup.exe`、`WindChime-win32-x64-0.6.2.zip`、nupkg、RELEASES 与 `SHA256SUMS.txt`。最终核验报告记录包版本、运行文件与当前构建的字节比对、校验和及安装动画嵌入结果。

本次打包目录为 `C:\Users\ASUS\AppData\Local\Temp\windchime-make-DT6zFl`。`package-verification.json` 确认 ASAR 中 17 个条目符合白名单，15 个运行文件逐字节吻合，未包含环境文件、设备凭据、数据库、网关或测试脚本。只读提取 Setup 内的 `background.gif`，与当前玻璃动画完全一致，1,599,060 字节。四件产物的 SHA256、尺寸以及 RELEASES 引用均通过。

| 文件 | 大小（字节） | SHA256 |
| --- | ---: | --- |
| WindChime-Setup.exe | 157466112 | `063062162118A232C8FFFDB6CE27F181782FB8677D8613F32454F75A461277B4` |
| WindChime-win32-x64-0.6.2.zip | 160174109 | `3BC6F0B49F1AF32DF1CD0F51E68E15899C786DF18CE6C9F7C6EA89B178150EAB` |

构建期间 Forge 的传递依赖提示 Node `fs.rmdir` 未来弃用警告，未导致失败；本轮未为此更换依赖。

本轮执行了源码 Electron 启动与真实界面测试，没有执行新版安装、覆盖安装或托盘卸载。此前自动审批拒绝了安装清理／重装操作，只返回 `blocked by policy`；没有改换工具绕过。安装往返仍未验收，不能把包构建和静态资源核验视为安装通过。

本轮没有重新操作 OBS 或直播姬，窗口捕获和透明采集沿用独立历史报告的版本边界；此次控制台换肤不等于采集复验。也未声称完成 B 站官方接入或平台上架。
