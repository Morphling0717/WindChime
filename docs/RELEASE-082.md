# 风铃 0.8.2 修复与验收记录

记录日期：2026-09-20（北京时间）。共享库与桌面均为 0.8.2，沿用已确认的玻璃界面、Logo、六种排版及 MIT 许可。实际安装、包启动、窗口采集和生产更新分别记录，不互相替代。

## 修复了什么

- **慢网络下列表一直刷新不出来**：周期刷新曾不断取消仍在进行的请求。现在后台刷新等待当前请求完成；主动操作仍会刷新，新话题不会接收旧话题的迟到结果。
- **连接失败却丢失当前选择**：连接、切换和移除现在先完成加密文件的原子保存，再切换内存状态及关闭旧输出。磁盘写入、重命名或加密失败保留旧连接。私人主窗口和磁贴的未保存稿在提交时统一检查，避免遗漏或重复确认。
- **旧浏览器配对迟到覆盖新连接**：绑定当前操作和网站版本，旧响应不能改回旧信箱。已兑换成功但本地保存失败的授权保留在本次配对内存里，用户可以明确重试；不会循环弹窗或重复兑换一次性凭据。
- **旧图片响应把新信件撤下**：图片成功及失败均绑定原展示、连接和快照版本；切换后的旧 409、超时和图片不能影响新画面。当前授权真正失效仍清空。
- **审阅图片尺寸错误**：私人预览读取已规范化图片的实际宽高，并正确取消旧加载、释放地址；预览和观众画面使用同一比例。
- **删除另一封信也清空正在播出的信**：数据库触发器只撤下受影响的当前快照，仍推进控制版本，防止旧上屏请求穿过修改。
- **撤销末尾信后“下一封”回到开头**：删除队列成员前保存其前驱位置，批量删除也保持正确进度。历史上已经丢失的位置按安全空白处理，主播可重新手动选择或结束展示后开始。
- **操作冲突提示一闪就消失**：轮询成功只清除连接错误，409 操作冲突保留到下一次明确操作或切换范围。不会自动重试上屏。
- **安装中断后的半安装状态**：玻璃向导保留，内部改用已校验的独立暂存目录、事务日志、目录切换和元数据恢复；故障注入覆盖进程在每个关键阶段退出及恢复时再次中断。完整实际安装往返仍需单独验收。
- **卸载遇到文件占用后无法重试**：先处理程序载荷，成功后才移除归属标记及卸载器；被占用时留下重试入口。旧的 DOS 8.3 注册路径和快捷方式按同一真实路径比较，避免把自己的旧安装误认成其他目录。

## 本轮实际执行

| 项目 | 结果 |
| --- | --- |
| 共享库 | `npm test`，238/238 通过，包括服务、权限、真实进程重启、慢请求、队列失效、图片比例和操作错误保留 |
| 共享分发 | `npm run pack:check` 通过；两站显式安装完全相同的最终 tgz，运行文件逐字节一致 |
| 两站 | Windows x64、Node 20.20.2；UliUli/Mia 生产构建、重复迁移和真实网页登录 HTTP 回归通过。UliUli 首次 Windows `0xC0000005` 崩溃记录保留，同源单独重跑通过 |
| 桌面主进程 | 生命周期 69/69 通过，含磁盘故障、保存时产生新草稿、旧配对结果、取消与原子提交竞争、旧图片错误隔离 |
| 桌面全量回归 | `node --test tests/*.test.cjs`，129/129 通过，包含上述生命周期、安装事务、NSIS 真实临时载荷、锁文件卸载重试、路径与许可测试 |
| 真实桌面管理 | Electron 44.3.0，332 次隔离模拟 HTTP；收件箱、设置慢请求、管理业务、外观及独立进程海报恢复通过，控制台错误 0 |
| 真实磁贴 | 实际 SQLite、HTTP 和多个沙箱 Electron 窗口；选信、未保存稿保护、话题切换、手动上屏、隐藏、409 提示跨轮询保留及独立进程恢复通过 |
| OBS 完整窗口采集 | OBS 32.0.1/WGC，16 项通过；透明、批准不播、隐藏、撤销、关闭重开、连续遮挡 70 秒及迟到隐藏请求通过 |
| OBS 故障采集 | 20 项通过；静默断网清空 **2655 ms**，渲染器卡死从最后确认请求起 **2841 ms** 清空；恢复保持空白，必须再次手动上屏 |
| 最终安装器载荷 | `npm run make`、`verify-package.cjs`、`verify-installer-stage.cjs` 通过；最终 NSIS 引擎真实解包 75 个文件逐字节匹配，旁边合成旧程序未变 |
| 最终便携 ZIP | `verify-portable.cjs` 通过；全新用户目录启动 0.8.2、Logo 和密钥界面正常，沙箱与权限隔离正确，不自动打开展示；关闭入托盘、重新打开及正常退出通过 |

OBS 完整轮次隐藏响应到空白 **136 ms**，被遮挡时远程隐藏 **591 ms**；网站隐藏响应故意延迟时本地紧急隐藏 **226 ms**。数值来自 OBS 源截图透明像素，未用 DOM 空白代替采集。两轮均未启动直播、录制、虚拟摄像头或回放缓存。

本机已有旧版风铃运行。OBS 的严格标题匹配忽略进程名，最初同名验收窗口关闭后被匹配到旧版窗口，因此首轮重开检查失败。测试改用带随机验收后缀的稳定标题，保持完整的关闭、重开与渲染进程恢复过程，原安装实例未被关闭或修改。两份最终报告明确记录测试标题；正式产品标题仍为 `WindChime Display`。同一台电脑不要同时保留多个同名展示窗口，否则需在采集软件重新确认目标。[OBS 32.0.1 匹配实现](https://github.com/obsproject/obs-studio/blob/32.0.1/libobs/util/windows/window-helpers.c#L400)

最终共享包 SHA-256：

```text
53ecc86171136ec4c4881f5db5841f5c87c284d3f600939852588e5b81bd846d
```

## 数据与升级

新增迁移标记 `0.8.2-live-queue-invalidation`，仅在迁移事务内替换失效触发器、添加队列位置维护触发器，不新增表或列，不改原信、话题、授权、批准快照、排序、已读或收藏。重复迁移、旧授权范围和撤销关系通过验证。服务端修复需要网站也更新；只更新桌面不会改变线上数据库触发器。

升级前保留数据库、图片、环境和非 GitHub 资源；回退不能用旧备份覆盖升级后收到的新信。UliUli 分支继续为 `codex/windchime-desktop-v0.6.0`，Mia 为 `codex/live-broadcast`，均已推送本次依赖和验证记录，未合并 main。两站原有未提交 `tsconfig.json` 字节保留。

## 尚未计为完成

- 实际 Windows 安装、升级、注册表与快捷方式往返、托盘运行时卸载，必须与临时事务目录故障测试分开。此前被自动审批拒绝的旧安装清理未绕过。
- 直播姬本轮启动停在 Windows 权限请求，尚未完成本版实际窗口采集；OBS 结果不能代替此项。
- 不包含默认代码签名、自动更新、B 站官方启动、H5 或审核上架。
- 生产 UliUli 初始空间仅约 1.6 GiB，需先完成旧镜像离机归档校验与容量预检；未达到构建门槛时保持现网不变。Mia 不部署。

最终 EXE：`WindChime-Setup-0.8.2.exe`，119135744 bytes，SHA-256 `d63a238c328daeac6c91a4847eea44120efae0967f049d8dd340749c377441ed`。

最终 ZIP：`WindChime-win32-x64-0.8.2.zip`，160661681 bytes，SHA-256 `5a88d5364afcf019b2a235498098f4f0526d82af3ba17095ac2ad396f2199459`。

证据：[共享测试](evidence/0.8.2/shared-tests.txt)、[分发检查](evidence/0.8.2/shared-pack-check.txt)、[两站报告](evidence/0.8.2/consumers-report.json)、[桌面 129 项测试](evidence/0.8.2/desktop-tests.txt)、[真实管理](evidence/0.8.2/desktop-management.json)、[磁贴](evidence/0.8.2/desktop-tiles.json)、[OBS 完整](evidence/0.8.2/obs-window-capture.json)、[OBS 故障](evidence/0.8.2/obs-faults.json)、[最终包结构](evidence/0.8.2/package-verification.json)、[完整暂存解包](evidence/0.8.2/installer-stage.json)、[便携启动](evidence/0.8.2/portable-startup.json)。本轮最终构建目录为 `windchime-make-x7L40V`，此前候选不作交付。
