# 0.8.3 源码与界面复核

## 最终发布复核（2026-09-24）

桌面和共享库均为 **0.8.3**，发布产物来自干净提交 `c4efb3dd2d7dab7fa432bc29350f0b3ed74b6a58`。其后的变更仅补充文档和验收证据，没有重打包或替换已验收字节。以下最新结论取代后文历史检查点的“待卸载／待升级／禁止发布”状态，历史失败记录仍保留。

- 新中文目录的实际安装通过；用户亲手卸载完成，并明确确认遇到“仍在运行，请先退出”提示，退出后继续成功。随后只读原生探针确认安装目录、注册信息和两个测试快捷方式均已移除，个人配置及原版安装未变。运行中提示的依据是用户报告，不冒称工具亲眼观察。
- 实际安装 0.8.2 后，使用本次 EXE 在同一隔离中文目录升级到 0.8.3，通过 75/75 载荷、注册信息及 Unicode 快捷方式检查。旧版保存的合成配置摘要完全保留，两个版本均实际启动并正常退出。见 [安装／升级／卸载证据](source-c4efb3d/native-lifecycle/)。
- 共享库 238/238、桌面 159/159 及确切运行源码的 Windows、Node 20/24 CI 通过。两站使用相同 TGZ，隔离生产构建、重复迁移和登录后的业务／权限 HTTP 回归通过；两站没有远端 CI，不能将本地结果说成远端 CI。
- OBS 的实际窗口捕获通过，隐藏 HTTP 响应后 573／602ms 清屏，静默断线 2947ms 清屏。直播姬实际透明、两图固定、隐藏、关闭重开和断线恢复不重播通过；HTTP 隐藏后清屏上界 729ms。全程仅本地预览。

**未完整验收：** 直播姬静默断线的 3 秒精确时限仍不确定；第一次截图横跨截止时刻，不能推断实际取帧时间。用户已实际休眠／唤醒，观察到 suspend 和唤醒后空白，但测试工具在过渡时断开并结束测试进程，不能算作完整的持续运行恢复验收。见 [直播姬本次记录](source-c4efb3d/livehime-native/root-observation.json)。原先旧安装环境的卸载弹窗根因没有完全证明，旧安装未清理；本次新装、升级、卸载均已有独立成功证据。

用户在获知这些限制后明确要求“如果没有大问题的话先发布”。目前未复现新的严重业务缺陷，据此完成发布复核并保留上述限制，**不宣称零 bug 或所有原定验收通过**。代码签名、自动更新和 B 站官方接入不属于此版本。

UliUli 的生产部署另行处理：最新只读检查在线仍为 0.8.1，服务器可用 7870 MiB，未达到既定的构建余量 8192 MiB，且尚须容纳备份及构建上下文。当前未停站或修改生产数据库。Mia 本轮不部署。

## 历史检查点（以下为当时记录）

本目录按源码提交分别记录源码测试、隔离 Electron 联调、ZIP 的实际 OBS 窗口采集和真实系统热键操作。**当前 c4efb3d 是诊断／兼容修复候选，仍为 `built-pending-native`，不是最终验收通过或最终冻结的发布包。** 安装／升级／卸载及直播姬采集门槛仍未全部完成。

**原生卸载验收仍未解决：用户手动运行旧 6b53289 测试安装的卸载器时遭到拒绝。** 后续正常双击环境未读到该测试安装的注册记录，不能把它笼统认定为已被短路径修复解决。当前 c4 已实际完成新中文目录安装并启动至托盘，正在等待新卸载器的运行中提示及正常卸载验收；发布门槛仍未通过。

此前完成界面和采集检查的桌面源码为 `83d284f2dfa00cc7d9ca94d932717979aff163f6`，tree 为 `6e569f3c770feecf824cd668b6898f468d73c16d`。`source-83d284f/`、`source83/` 和 `checkpoint-6b53289/` 的原始报告保持原来源，不能重标为新候选已经完成的验收。桌面和共享库版本仍均为 0.8.3。

## c4efb3d 诊断／兼容候选：构建与 CI

新证据单独保存在 [source-c4efb3d](source-c4efb3d/)，源码为 `c4efb3dd2d7dab7fa432bc29350f0b3ed74b6a58`，tree 为 `dbf10f9587d0095fa16e3d22d30e557cadbdd970`。TGZ、EXE、ZIP 均从这个干净的独立工作树生成，原候选文件没有覆盖。短路径兼容和拒绝原因诊断已进入此候选，但**不能宣称它解决了用户实际卸载弹窗**；真实根因仍在调查。

| 检查 | 当前候选的实际结果 |
| --- | --- |
| 本地源码测试 | 共享库 238/238、桌面 159/159 通过，失败和跳过均为 0；Node 22.22.3、npm 10.9.8、Windows x64。 |
| 精确新 TGZ | 新临时消费者实际安装，lock 完整性、包内 0.8.3／MIT、八个无界面入口、全新 SQLite 和严格 TypeScript 消费者通过；未复用旧共享包验证。 |
| CI | push [35896649015](https://github.com/Morphling0717/WindChime/actions/runs/35896649015) 和 PR [35896654423](https://github.com/Morphling0717/WindChime/actions/runs/35896654423) 均 completed/success，实际 headSha 均为 c4efb3d；两次 Windows 各 159/159，Node 20/24 作业均通过。Node 20 的 Docker 步骤按条件跳过，Node 24 Docker 构建通过。 |
| 成品包后台验证 | EXE／ZIP 内容、许可、载荷对应验证通过，stage-only 引擎解包 75 文件匹配，合成哨兵不变；没有实际安装、注册程序或运行卸载器。 |
| 首次 SDK 检查与恢复 | 初次检查因隔离依赖中 `electron/dist/LICENSE` 缺失而停止。显式补齐锁定 Electron 44.3.0 SDK 后，对同一份 EXE／ZIP 重新验证通过；恢复前后摘要不变，没有重新打包。`BUILD-FAILED.json` 和原错误日志保留，恢复记录另存。 |
| 运行文件比较 | 实际读取新旧 staging 中的 EXE、app.asar 并计算 SHA-256，各自先匹配其载荷清单，再确认两者相等；不是只比较报告声明。此结果不证明新安装器可正常卸载，不把旧 OBS／直播姬／UI 报告移给新源码。 |
| 新 ZIP 展示布局与独立进程重启 | 主验收线程实际运行新 ZIP：六种排版 × 三主题共 18 组、另 10 项检查通过，150% DPI；正文滚动、图片固定且保持比例，两次独立进程保留批准记录但重开空白。新报告明确 `nativeCaptureValidated=false`、`obsStarted=false`、`realKeysPressed=false`，不计作直播软件采集或实际热键验收。 |
| 两站新包 | UliUli／Mia 固定同一 c4 TGZ，隔离生产构建、fresh／legacy／current 各两次迁移、原登录邮件 HTTP 回归、每站 9 项权限检查及双向跨站拒绝通过；开发分支已推，PR 仍为草稿，未合并、未部署。远端没有 CI 记录，不能称两站 CI 通过。 |
| 新 EXE 实际安装与启动 | root 从 Explorer 启动玻璃向导，选择新中文目录及两个快捷方式；原生 V2 只读探针确认 75 文件、marker、registry、shortcut 严格匹配，5 项保护文件及原 Squirrel 记录不变。安装后真实程序启动、窗口／托盘检查通过，目前留托盘，`gracefulExit=false`；尚无卸载结果。 |

| 新候选资产 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `WindChime-Setup-0.8.3.exe` | 119133184 | `d9d5dff2fe9bb1c7c9af1f81ada17289d80dba0f2989753a1b3d1d932fc60ef4` |
| `WindChime-win32-x64-0.8.3.zip` | 160660617 | `8f7b25bddbca1fa32ec618291ab476968c9153016e584dfb93b49f5c12a4127f` |
| `windchime-embed-0.8.3.tgz` | 54912376 | `a79abe42607b314f932f32618722528079842b59588e01cabf883936f8ae28f3` |

新 TGZ 包含新增文档／证据，字节与旧包不同；两站已使用带完整 c4efb3d 提交后缀的新 vendor 文件并重新验证，旧 0.8.3 文件保留。两站提交／草稿状态和本地验证见下节，不代表生产部署。源码／工具／锁文件及三产物摘要见 `source-tools-and-assets.json`；字节比较见 `runtime-equality.json`。公开文本已去除个人路径，公开副本与原报告摘要分开列入 manifest，不得拿规范化副本替换本地来源协议中的原文件。

### 两站 c4 新包验证与草稿 PR

`source-c4efb3d/consumers/` 记录 UliUli `a32e763da57b14e4df49033e789c5ea402fb22db`、Mia `4085a0a26bfe904e12328db345a885f1e5a74c8c`。两者固定 `windchime-embed-0.8.3-c4efb3dd2d7dab7fa432bc29350f0b3ed74b6a58.tgz`，摘要与本候选一致；锁文件只更改风铃条目，旧包和原工作区未提交 tsconfig 均保留。首轮 npm 引入无关可选依赖变动被检查拒绝，恢复原条目并重跑 npm ci 后通过；原失败记录另存，未隐去。

Node 20.20.2、npm 10.9.8 的新隔离工作树中，两站生产构建、三种数据库各两次迁移及完整邮件 HTTP 回归通过。每站另 9 项登录／授权／缓存响应检查通过，管理 Bearer、展示 Bearer 和 Cookie 双向跨站访问均拒绝。图形浏览器交互与真实 Service Worker 离线缓存没有在这组 HTTP／VM 检查中验收；构建警告保留在日志中。

两站已分别推到原开发分支；[UliUli #1](https://github.com/Morphling0717/Next_UliUli/pull/1)、[Mia #1](https://github.com/Morphling0717/Next_Mia/pull/1) 的 18:03–18:04 UTC 快照均为 open、draft、未合并、无自动合并。远端 workflow、check-run、commit-status 列表为空，**这里是本地验证通过，不是远端 CI 通过**。未访问或部署生产环境；`final-review.json` 的未提交状态属于推送前时点，后续实际提交／推送以 `commits.json` 和 `pull-requests-final.json` 为准。

主验收线程另报告：同一份无删除诊断从命令行启动能读到隔离测试安装记录，从 Explorer 双击则在第一项 `HKCU InstallLocation` 读取处失败，路径／目录树检查尚未执行。只读对照显示两者 SID 摘要相同、均为 32 位且没有 package identity，但相同 HKCU／HKU 键在命令行两视图查询成功，Explorer 探针返回不存在。此处仅记录主线程交接观察，未公开私人键名、SID 或截图，详见 `uninstall-investigation-observation.json`；**尚未确定原因，更未证明原故障已修复。**

新 ZIP 的独立渲染验收发生于 2026-09-23 17:51:35–17:52:02 UTC，完整报告和 20 张原始 PNG 另存 `source-c4efb3d/display-matrix/`。报告内构建、包检查、载荷清单、EXE 和 ZIP 摘要均已与当前候选复核。整理时实际查看纵向 Mia、双栏 UliUli 和重启空白图，未见私人控件；正文是合成测试输入，不是产品默认文案。此项确实重跑新包，未复用旧矩阵截图，也不外推未完成的安装／卸载或直播软件采集。

### c4efb3d 新 ZIP 的实际 OBS 窗口采集

主验收线程随后实际运行新 ZIP 的两个独立进程及隔离合成配置，分别完成 `full` **17 项**、`faults` **21 项**，均正常退出。新证据保存在 `source-c4efb3d/packaged-obs/`，运行 EXE／ASAR、ZIP、build-info 摘要均匹配本候选；没有把旧 83d284f 的 23 项故障运行拿来替代本次 21 项。使用 OBS 32.0.1、WebSocket 5.6.3，未开播、录制或启用其他输出，B 站／网关请求为 0，合成授权已撤销。

完整运行保留 **34 条实际采样记录、14 张最终命名画面**；故障运行保留 **96 条采样记录、20 张最终命名画面**。原报告截图数组中的重复文件名表示连续采样，公开 PNG 是该名称最后一次保存的画面；所有记录均保留，没有把 PNG 文件数当作全部采样数。另有两张棋盘格背景文件，不计入采样数。

| 本次测量 | full | faults | 起点与范围 |
| --- | ---: | ---: | --- |
| 本地隐藏返回至采集空白 | 145 ms | 160 ms | 桌面 hide IPC 调用返回，不冒称独立服务端 HTTP 完成日志 |
| 本地隐藏发起至采集空白 | 356 ms | 348 ms | 含整个调用往返，均小于 1 秒 |
| 遮挡时远端隐藏返回至空白 | 573 ms | 602 ms | 直接管理 HTTP 调用返回后的客户端计时 |
| 网站响应延迟时本地紧急隐藏 | 225 ms | 215 ms | 本地命令发起至实际采集空白 |
| 遮挡保持时间 | 70030 ms | 10 ms | 只有 full 执行约 70 秒遮挡 |
| 静默断线至实际采集空白 | — | 2947 ms | 从最后已确认帧请求发起计算，小于 3 秒 |
| 渲染冻结至实际采集空白 | — | 2859 ms | 从最后已确认帧请求发起计算，小于 3 秒 |

原故障报告另保留 `elapsedMs=2736`、`fromFreezeMs=2775`，它们分别从断线／冻结操作起算，未拿来替换较早的最后帧起点。实际查看透明合成、断线空白和冻结空白图，画面仅含合成审阅内容及测试背景，无私人控制台或错误。关闭重开、撤销、恢复后不自动重播均通过。本次 `interactive=null`，没有实际系统按键；也不证明 c4 的直播姬 3 秒时限、真实系统休眠或安装／卸载。

### 实际安装前基线与后续结果

主验收线程的新原生只读基线显示：正常 Explorer 双击环境下，两个新安装器测试注册记录均不存在，原 Squirrel 0.8.0 记录仍在；两个测试快捷方式存在，目标仍指向旧 6b 隔离安装，文件摘要与命令行读取相同。当前拒绝的直接条件是**正常双击环境没有该测试安装的注册记录**，不能笼统归为路径校验已修复。命令行上下文此前为何能读到这些键仍在调查。

为避免覆盖遗留快捷方式，`CaptureLink` 会停止安装。主线程当时请用户只手工删除本轮两个测试 `.lnk`，保留原 WindChime；该等待时点观察仍保留在 `packaged-obs/native-install-baseline-observation.json`。**用户随后已亲手完成删除**，18:03:00 UTC 原生基线显示两个测试快捷方式均不存在，原 Squirrel 0.8.0 记录仍在；旧 6b 临时文件未动，旧自动清理审批拒绝没有绕过。

18:04:59 UTC 的原生安装前检查为 `state=absent`、`readyForNativeInstall=true`：候选 Setup 摘要核对通过，两个测试安装注册记录和两个测试快捷方式均不存在，5 项原保护文件及原 Squirrel 记录不变。root 随后从 Explorer 启动 **本次 c4 EXE 的玻璃向导**，经过欢迎／MIT 许可／选项页面，选择全新中文目录并勾选桌面和开始菜单快捷方式，完成实际安装。

第一次安装后 V1 探针误查根目录 `windchime-install.ini`，因此报告 marker 不匹配；它同时已经报告 75 个载荷文件、注册信息及快捷方式匹配。核对探针源码后，将测试探针定位改为真实的 `resources/windchime-install.ini`；V2 仍严格核对 AppId／Version，没有放宽身份检查。18:12:19 UTC 原生 V2 结果为 `state=installed`：75/75 文件、marker、registry、shortcuts 全部匹配，5 项保护文件和 Squirrel 记录未变。**这是验证探针的路径错误，不是新安装器产品缺陷**；V1 失败及 V2 结果分别保留在 `source-c4efb3d/native-install/`。

安装后的实际 EXE 已核对来源并启动，连接界面和 Logo 正常，只有私人窗口、没有自动展示；沙箱／上下文隔离开启，Node 集成关闭。关闭到托盘、重新打开、再次留托盘通过。`runtime-ready-before-uninstall.json` 保存 18:12:42 UTC 的就绪检查点，明确 `gracefulExit=false`；`passed` 只指其中列出的启动／安全／界面检查。root 已请用户亲手启动新目录的卸载器测试运行中提示，截至本检查点尚未启动、没有卸载结果。未绕过历史电脑操作阻止。

未公开含个人路径的安装向导截图、active-runtime 指针、端点／nonce、配置或原始私有基线。公开的 V2 明细按白名单保留载荷与各项严格匹配结果，私人文件摘要省略；manifest 另外保留原始私有报告摘要以追溯来源。正常卸载、升级和完整原生门槛仍待完成。

以下其余界面、采集和按键段落描述 83d284f／6b53289 的历史运行；其中当时称为“最终”的候选不代表当前 c4efb3d 已通过原生验收。

## 83d284f／6b53289 检查点的实际结果

| 验证 | 结果与范围 |
| --- | --- |
| 共享库本地全量测试 | 检查点 6b53289：238/238，通过；最终提交仅修改 5 个桌面文件，共享源码与精确 TGZ 均未改变。 |
| 桌面本地全量测试 | 最终提交 83d284f：145/145，通过；Node 22.22.3、npm 10.9.8、Windows x64。包含构建与 Windows 安装器回归，但不代替最终 EXE 的人工安装。 |
| 桌面管理真实服务联调 | 最终提交：16 项、315 次请求，通过；真实 SQLite/HTTP、隔离合成信件，覆盖密钥导入、话题同步与选择、读取/收藏/CSV、分享保存、关键词设置、授权列表同步、脏稿保护及外观预览。第二个独立 Electron 进程验证海报设置恢复与实际 PNG 保存；不会恢复展示窗口。 |
| 展示窗口尺寸联调 | 最终提交：5 项、49 帧、2 次接收连接，通过；固定图片、文字滚动、短窗口、高字号与重新打开空白。使用合成只读 IPC 帧，**不含 OBS 或直播姬采集**。 |
| 磁贴与下一封逻辑 | 检查点 6b53289：11 项、233 次请求，通过；5 个磁贴就绪后原生置顶均为 true；包含权限限制、未保存稿、冲突、批准不播放、上屏/撤销/下一封、切换隔离及第二独立进程重启。快捷键注册经测试拦截，调用的是实际回调；**未发送操作系统按键**。 |
| GitHub CI | 最终提交 push `35885408854`、PR `35885412788` 均 completed/success；各自 `verify (20)`、`verify (24)`、`desktop-windows` 均成功。Node 24 Docker 构建成功；Node 20 的 Docker 步骤按工作流条件跳过。旧检查点两次 CI 另存作来源记录。 |

主要命令：共享库 `npm test`、`npm run pack:check`；桌面目录 `npm test`，以及 `electron.exe scripts/management-smoke.cjs`、`electron.exe scripts/tiles-smoke.cjs`、`electron.exe scripts/display-viewport-smoke.cjs`。各脚本使用隔离测试配置和合成数据。CI JSON 包含逐步结果和时间。

## 文案与截图复核

6b53289 初次实际截图仍有“你的创作空间”“每一封，都由你决定”两句侧栏文案；它们和多余的安装器英文装饰标题已在 83d284f 删除。Logo、品牌名、操作名称、防误播说明、连接失败及保存冲突提示保留。未改写用户来信、昵称或数据。

实际查看最终截图 `inbox-1200x620`、`studio-961x700`、`settings-wide`、`appearance-mia-narrow`、`appearance-uliuli-narrow`，以及两张展示尺寸截图。上述口号已消失；侧栏无残留空段，网站与话题选择、隐藏按钮没有遮挡，窄窗口的主题选项与预览可读。预览昵称、正文和图片说明为中性示例。主题名称保留在私人设置中，观众内容未添加主题名称。

管理截图中的 `Ctrl+Shift+H 快捷键注册失败` 是本轮另一份隔离安装测试程序仍在托盘运行造成的占用，属于私人控制台错误提示；它没有进入展示内容。不能据此声称实际系统热键已经通过。海报中的合成标题、署名为测试输入，不是产品默认口号。

## 精确共享包与源码对应

共享包 `windchime-embed-0.8.3.tgz`：31,415,194 字节，SHA-256 为 `c1207a59e8a3d5f9fe9e4e9a6c1895250569f2985add9b08c9737d8170a8f865`。原始安装、类型和 SQLite 验证报告 `shared-package-verification.json` 仍明确归属 6b53289，未改写或替换。

在干净、已提交的 83d284f 隔离工作树重新执行 npm pack，得到**逐字节相同**的 TGZ；详见 `shared-package-reproduction-83d284f.json`。这证明新桌面源码仍能生成网站已固定的同一共享包，并不声称原先的共享包验证是在新提交上重跑。两站的验收由独立 `consumers-report.json` 记录。

最终候选的后台来源记录位于 `source-83d284f/candidate/`。候选目录已具有四份发布资产、三载荷校验清单、桌面包独立验证报告、原始共享包验证报告及重新打包证明。来源协议为 schema 2、状态 `built-pending-native`；发布校验器仍按预期拒绝发布准备。后台便携包预检仅核对包内字节与适配器语法，`startedAnyApp=false`，不代表最终程序已经启动。

候选共享包报告的 `sourceCommit` 记录这次可复现来源 83d284f，`verificationSourceCommit` 保留真实执行安装、类型和 SQLite 验证的 6b53289；原始报告与重新打包证明都有独立摘要引用。公开副本经过文本规范化，来源协议内报告摘要仍指向候选目录的原始字节，不能拿公开副本直接替换本地发布报告。

## 最终便携包的 OBS 实际采集

2026-09-23 16:09–16:11 UTC 的两个独立运行已归档到 `source-83d284f/packaged-obs/full/` 和 `faults/`。两次均启动最终 ZIP 解压出的 WindChime.exe，使用独立合成配置、真实管理桥接导入、实际 OBS 32.0.1 窗口捕获和 OBS WebSocket 5.6.3；并未使用源码开发版代替成品。程序 EXE、app.asar、ZIP 及构建信息的摘要全部与最终候选预检一致。

- ZIP SHA-256：`f3b63a79606ce6ebc2797293442a6b7d9460ef9b002b1d545bbda4331365cf9c`
- 运行程序 EXE SHA-256：`9fe566330366bfd92cd24f74e34ab66914c6fa359d014f6917a0551352507987`
- app.asar SHA-256：`528a092fbe10d5ea5f0dae7bea759b5967092a4ea243e9463e66fa2e964264ff`

完整采集 17 项通过，故障采集 23 项通过。验证未审核空白、仅批准仍空白、手动上屏、透明背景、隐藏、撤销、关闭和重新打开；输出关闭后没有采到私人控制台。完整采集还实际覆盖遮挡展示窗口 70,031 毫秒后继续控制。故障运行包含静默断线、渲染进程冻结及恢复后保持空白。两次均未开播、录像或启动其他输出，B 站及网关请求为 0；测试授权撤销，WindChime 程序正常退出。主验收线程报告 OBS 清理仅针对测试脚本自己启动的子进程。

| 实际采集计时 | 完整运行 | 故障运行 | 门槛 |
| --- | ---: | ---: | --- |
| 隐藏响应后清屏 | 146 ms | 136 ms | ≤ 1,000 ms |
| 远端隐藏响应后，遮挡窗口清屏 | 576 ms | 572 ms | ≤ 1,000 ms |
| 网站响应延迟时，本地紧急隐藏命令至清屏 | 212 ms | 216 ms | 不等待网站响应 |
| 静默断线：最后确认状态对应帧发出至实际采集空白 | — | 2,896 ms | ≤ 3,000 ms |
| 渲染冻结：最后状态帧发出至实际采集空白 | — | 2,877 ms | ≤ 3,000 ms |

已实际查看上屏源、透明合成、静默断线空白和冻结清屏 PNG；画面仅有合成审阅内容及其图片，无私人控制台、通知或错误详情。报告中的重复截图名称代表连续采样观察，归档 PNG 是该名称最终保存的一帧；计时来自原报告，不能把一张静态图当作时序证明。

交互测试记录明确为 `hotkeysActuallyPressed=false`。当时电脑操作工具看到 Windows 安全中心的 OBS 防火墙对话框，未操作安全对话框、也未发送系统热键；不能把回调测试或快捷键配置当作实际按键通过。此次没有直播姬采集通过记录。

## 最终程序的主题、排版与 150% DPI

`source83/display-matrix/` 归档的是安装验收线程已完成的最终程序渲染测试，整理时没有重跑测试。源码仍为 83d284f；报告内 EXE、ZIP、运行程序和 app.asar 摘要与前述最终候选一致。2026-09-23 16:23:36–16:24:00 UTC，两次独立打包程序进程使用隔离配置，实际显示缩放均为 **150%**，并非将源码浏览器截图作为成品截图。

18 个组合全部通过：`stack`、`split`、`banner`、`sidebar`、`portrait`、`focus` 六种排版，分别搭配 `pure`、`uliuli`、`mia` 三种主题。测试字号为 26 或 32 px。各组合的正文均实际发生滚动、图片位置保持固定，横向无溢出，展示窗口没有私人控件或私人提示。两幅合成图片分别为 16:9 和 2:3，按原图比例显示。

另外 10 项检查通过，包括新投稿和批准后保持空白、独立第二进程仍保留批准队列和连接、重开输出不恢复旧信、再次手动上屏正常。两次进程仅访问合成站点；未使用生产数据，未改变网络权限，测试授权已撤销。

归档包含完整报告、18 张组合截图、隐藏与重启空白截图以及总览图 `contact-sheet.png`。实际查看总览可见横向、双栏、侧栏和纵向排版分开呈现，图片未跟正文一起滚动的结论来自报告的前后几何检查。总览上方的英文组合名是测试图注，不属于观众展示 UI；合成正文也不是产品默认文案。

本项是原生展示窗口渲染检查，明确保留 `nativeCaptureValidated=false`、`obsStarted=false` 和 `realKeysPressed=false`，不重复计作 OBS／直播姬采集或系统热键通过。随后单独完成的真实热键操作见下一节。

## 单独的真实系统热键操作

`source83/system-hotkeys/` 包含一份明确归属主验收线程工具观察的报告，以及两份原始合成站点状态快照。程序仍对应最终 83d284f 候选，资产和运行文件摘要已复核。这次主验收线程实际使用 `sky.press_key`，目标为 WindChime 私人控制窗口；没有通过脚本回调冒充键盘输入。

- 2026-09-23 16:35:27.274 UTC 发送 `Control_L+Alt_L+Next`（配置为 Ctrl+Alt+PageDown）。主验收随后读取原生展示窗口文字为“合成投稿人二／第二封合成来信 · 真实快捷键验收”，站点当前信件变为第二封 `35261d84-22a9-42b6-b7b0-5a5a561ac4f2`。
- 16:35:52.883 UTC 发送 `Control_L+Shift_L+h`。当前信件和快照均变为 null；主验收读取原生展示窗口，确认没有来信文字。

两项实际按键检查通过。报告将“主验收线程发送按键和读取原生窗口的观察”与“本次整理时核对的已保存站点状态”分开注明。保存快照的更新时间分别为 16:35:41.582 和 16:36:04.244 UTC，晚于主验收最初观察时间；这些文件证明结果状态，不用于推算毫秒级响应速度。

本次电脑操作截图异常带出了不相关的 Codex 背景，因此未公开、未用作证据；仅使用按键工具观察、只读窗口文字检查和合成状态。此验收目标是实际私人窗口，不额外宣称在其他全屏游戏持有焦点时也已验证。此前 OBS full/faults 和渲染矩阵报告的 `hotkeysActuallyPressed=false`／`realKeysPressed=false` 均保持原值，因为那些运行没有发送系统按键。

## 直播姬真实窗口采集（部分验收）

用户已在直播姬 7.54.0.10521 中添加合成展示窗口捕获，并把专用棋盘格背景放到捕获源下方铺满画布。实际只做本地预览，未开播。新增报告、原始计时和 10 张经过逐图确认的画布裁图保存在 `source83/livehime-capture/`。整理过程只提取原生截图的预览区域（x=284、y=198、1139×639），不修改画布内容，不公开原截图或私人房间信息。

已单独通过 9 项：初始长信和两张图正常捕获、棋盘格能透过信件四周、隐藏清空、关闭展示清空、重开保持空白、新手动上屏自动重新捕获、断线后最终空白、恢复连接仍空白，以及恢复后必须新手动上屏。观众画布内没有私人控制台或错误详情；重开窗口不需要重新配置直播姬捕获来源。

隐藏按真实 HTTP 响应计时：`POST /api/mail/live/control/action` 请求 630 的响应完成时间是 `1790182901985`，返回已空白原生截图的时间为 `1790182902938`，因此响应至已清屏的**保守上界为 953 ms**，满足 ≤1 秒。原 `timed-hide.json` 中 637 ms 从测试助手的 command-result 完成时间起算，不是 HTTP 响应时间；原数据不改，另用解释字段说明。HTTP 请求与响应对应原日志第 1265–1266 行，摘录、行摘要与两种时间均保留。

**第一次静默断线的 3 秒采集门槛仍为 inconclusive。** 从最后成功帧请求发起计，截图调用在 +2762 ms 开始、+3207 ms 返回，早期采样仍有来信；渲染观察在 +2879 ms 已无卡片。截图实际取帧时刻不明，这组数据既不能证明直播姬在 3 秒内清屏，也不能证明产品超时。稍后的真实画布空白及恢复不重播已通过，但不能代替时限验收。原计时与渲染观察另存，未把渲染器状态冒充采集画面。

只有 root 逐张确认的真实直播姬截图进入 SHA-256 白名单后才可裁图。误返回 Codex 的画面、带文件对话框的画面以及没有结论的早期空白帧均未公开、未用于通过判定。953 ms 隐藏通过不能外推断线时限；本节没有宣称完整直播姬 QC 已通过。

## 尚未完成的原生验收

初次原生操作时，卸载程序的电脑操作被工具返回 `product policy blocks this app` 阻止，直播姬也曾停在 Windows UAC；没有改用其他工具绕过。**随后用户已处理 OBS 与直播姬安全弹窗**，因此 UAC 不再作为当前未处理项。直播姬一度退出未显示主界面；最新操作通过 Explorer 打开现有开始菜单的正常快捷方式后，**直播姬 7.54.0.10521 主窗口与预览界面已成功出现，未开播**。

当时直播姬交互受 Windows 权限层级限制：电脑操作工具的 `get_window_state` 明确返回 `Accessibility is limited because the target window has higher Windows integrity than the Computer Use helper.`。主验收按截图尝试点击场景添加控件两次，未获得响应；没有提权、改变安全设置或换其他控制工具绕过。随后用户已手动配置合成窗口捕获和棋盘格背景，才完成上一节所列的部分实际采集检查；**静默断线 3 秒时限仍待确认**。

启动阶段的工具限制原样保留于 `source83/livehime-observation/interaction-limited.json`，它是历史时点记录，不能当作用户后来没有配置捕获。没有公开包含私人房间标题的全界面截图，旧证据内容未改。

用户随后亲手启动旧 6b53289 测试安装的卸载器，主验收通过只读窗口确认弹窗：“无法安全确认这份风铃的安装目录。已停止卸载，个人设置未被删除。请检查安装位置是否被移动或包含目录、文件链接。”后续按 CIM 与日志核对，卸载器在 17:13:05 UTC 启动（精度到秒），采集测试进程在 17:13:38.968 UTC 正常结束，故不能说用户启动卸载器前测试程序就已退出；只能确认检查错误时已经退出。弹窗仍明确属于前置目录校验，不是运行中提示。现为**已复现的卸载目录验证失败，根因待定位和修复**，不再仅归为自动操作政策阻塞。独立观察记录见 `checkpoint-6b53289/native-uninstall/known-failure.json`。主验收说明 83d284f 沿用相同卸载源码，但尚未对最终 83 安装单独复现这个失败，不能把潜在影响写成已经实测。

最终候选 EXE 的安装、升级、卸载、真实系统休眠恢复及直播姬 3 秒断线清屏时限仍由主验收流程负责。最终 ZIP 的 OBS 采集、上述范围内真实系统热键和直播姬部分采集已单独通过，不能代替其他门槛，不据此设置 `final-reviewed`、正式发布或合并。

生产检查只读：主验收线程报告剩余 8,293,748,736 字节，小于既定 8 GiB 门槛，运行中站点健康检查为 200。本次没有部署或清理生产数据；服务器地址与个人主机信息不写入公开证据。

## 文件完整性与脱敏

另补充旧检查点的实际首次安装记录到 `checkpoint-6b53289/native-first-install/`，仅包含已明确可公开的安装前／安装后保护核对、安装后运行验证三份 JSON，原始字节未改。归属源码 **6b53289**，对应旧安装 EXE 为 119,133,696 字节，SHA-256 `8ed71daf4ae8b386780b3a58f3219140934633abc42c004d0a9f05237c429ad8`。三份报告记录原有文件和配置未变、隔离目标注册信息及快捷方式符合预期，安装后的私人窗口、Logo、沙箱、托盘关闭重开及正常退出通过；未自动恢复展示。该候选随后被文案补漏提交 83d284f 取代，**不计作最终 83d284f 安装、升级或原生卸载通过**。未复制 baseline、active-runtime、配置、nonce、私人验证资料或包含桌面背景的电脑操作截图。来源及旧 EXE 摘要另外写入索引的 `supersededNativeFirstInstall`。

`source-qc-manifest.json` 列出收集时间、来源提交、输入文件和原始 SHA-256，以及公开副本的字节数与 SHA-256。源码联调文本替换个人目录、JSON 重排为 LF，因此两种摘要分开记录。OBS 的两份 `final-binary-verification.json` 和全部 PNG 保持原始字节；OBS `report.json` 仅移除临时站点地址并规范 JSON。局部 `.gitattributes` 设置 `-text`，防止 Git 自动换行导致证据摘要失效。没有收集授权、端口、OBS 配置、交互命令文件或旧运行失败截图，也没有覆盖共享包和两站验证线程提供的文件。
