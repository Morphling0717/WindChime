# 风铃 0.8.3 验收与发布记录

## 最终发布复核（2026-09-24）

桌面和共享库均为 **0.8.3**，发布产物来自干净提交 `c4efb3dd2d7dab7fa432bc29350f0b3ed74b6a58`。其后的变更仅补充文档和验收证据，没有重打包或替换已验收字节。以下最新结论取代后文历史检查点的“待卸载／待升级／禁止发布”状态，历史失败记录仍保留。

- 新中文目录的实际安装通过；用户亲手卸载完成，并明确确认遇到“仍在运行，请先退出”提示，退出后继续成功。随后只读原生探针确认安装目录、注册信息和两个测试快捷方式均已移除，个人配置及原版安装未变。运行中提示的依据是用户报告，不冒称工具亲眼观察。
- 实际安装 0.8.2 后，使用本次 EXE 在同一隔离中文目录升级到 0.8.3，通过 75/75 载荷、注册信息及 Unicode 快捷方式检查。旧版保存的合成配置摘要完全保留，两个版本均实际启动并正常退出。见 [安装／升级／卸载证据](evidence/0.8.3-copy/source-c4efb3d/native-lifecycle/)。
- 共享库 238/238、桌面 159/159 及确切运行源码的 Windows、Node 20/24 CI 通过。两站使用相同 TGZ，隔离生产构建、重复迁移和登录后的业务／权限 HTTP 回归通过；两站没有远端 CI，不能将本地结果说成远端 CI。
- OBS 的实际窗口捕获通过，隐藏 HTTP 响应后 573／602ms 清屏，静默断线 2947ms 清屏。直播姬实际透明、两图固定、隐藏、关闭重开和断线恢复不重播通过；HTTP 隐藏后清屏上界 729ms。全程仅本地预览。

**未完整验收：** 直播姬静默断线的 3 秒精确时限仍不确定；第一次截图横跨截止时刻，不能推断实际取帧时间。用户已实际休眠／唤醒，观察到 suspend 和唤醒后空白，但测试工具在过渡时断开并结束测试进程，不能算作完整的持续运行恢复验收。见 [直播姬本次记录](evidence/0.8.3-copy/source-c4efb3d/livehime-native/root-observation.json)。原先旧安装环境的卸载弹窗根因没有完全证明，旧安装未清理；本次新装、升级、卸载均已有独立成功证据。

用户在获知这些限制后明确要求“如果没有大问题的话先发布”。目前未复现新的严重业务缺陷，据此完成发布复核并保留上述限制，**不宣称零 bug 或所有原定验收通过**。代码签名、自动更新和 B 站官方接入不属于此版本。

UliUli 的生产部署另行处理：最新只读检查在线仍为 0.8.1，服务器可用 7870 MiB，未达到既定的构建余量 8192 MiB，且尚须容纳备份及构建上下文。当前未停站或修改生产数据库。Mia 本轮不部署。

## 历史检查点（以下为当时记录）

本轮桌面、共享库均为 0.8.3。当前诊断／兼容候选为 c4efb3d，状态 `built-pending-native`；真实卸载失败根因仍在调查，不能发布或合并。公开 API、授权范围和数据库结构没有新增。页面、安装器和预览样稿删除宣传口号，保留必要操作、错误和防误播说明；用户原信、上屏稿、保存的海报配置和许可证原文不改。

## 当前诊断／兼容候选 c4efb3d（待原生验收）

源码 `c4efb3dd2d7dab7fa432bc29350f0b3ed74b6a58`、tree `dbf10f9587d0095fa16e3d22d30e557cadbdd970` 的干净隔离工作树已生成新的共享 TGZ、安装 EXE 和便携 ZIP。该提交含短路径兼容处理、具体拒绝原因和相应回归；**尚无证据证明它解决了用户实际卸载失败**，也不代表最终候选已经冻结。

本地共享 238/238、桌面 159/159 通过；两个确切源码的 CI [push](https://github.com/Morphling0717/WindChime/actions/runs/35896649015)／[PR](https://github.com/Morphling0717/WindChime/actions/runs/35896654423) 中 Windows（各 159/159）及 Node 20/24 作业全部通过。新 TGZ 实际独立安装、SQLite、八入口、MIT 和 TypeScript 验证通过。包内容检查与 stage-only 75 文件解包通过；stage-only 本身不代表实际安装。**随后另完成了本候选的真实新目录安装及启动，升级／卸载仍待验收**，详见下文。

首次包检查因本地 Electron SDK 许可证文件缺失停止；补齐锁定的 Electron 44.3.0 SDK 后，对同一 EXE／ZIP 重验通过。原失败记录保留，恢复没有重新打包或改变产物摘要。新旧实际运行 EXE、app.asar 的 SHA-256 相等，但旧采集和 UI 报告保持原提交归属，不作为新安装器通过的证据。

随后已实际重新运行 **c4efb3d 的新 ZIP**：18 种排版／主题组合与另 10 项检查通过，实际 150% DPI，正文滚动而图片固定。两个独立成品进程保留合成连接和批准记录，重新打开展示仍为空白，直到新的手动上屏。新报告和截图见 [本候选展示矩阵](evidence/0.8.3-copy/source-c4efb3d/display-matrix/render-matrix-report.json)；它是程序渲染验收，明确没有启动 OBS、验证直播软件采集或发送实际热键。

另一次独立的 **新 ZIP 实际 OBS 窗口捕获**已完成：普通 17 项、故障 21 项通过，两次程序正常退出。未审核／批准空白、透明、手动显示、隐藏、撤销、关闭重开和恢复不重播通过；普通运行遮挡 70030ms。新报告实际保留 34／96 条采样记录、14／20 张最终命名画面，没有复用旧故障运行的 23 项计数。

本地隐藏发起至实际空白为 356／348ms；调用返回至空白为 145／160ms，此处返回点是桌面 IPC，不能冒称独立服务端 HTTP 完成日志。远端管理 HTTP 调用返回后空白为 573／602ms；网站响应延迟时本地紧急隐藏为 225／215ms。静默断线从最后确认帧请求发起到实际空白 **2947ms**，渲染冻结为 **2859ms**，均小于 3 秒。完整原始检查与帧记录见 [本候选 OBS 证据](evidence/0.8.3-copy/source-c4efb3d/packaged-obs/obs-capture-summary.json)。本次没有实际按键、直播姬时限、系统休眠或安装卸载验收；未开播、录制或启用其他输出。

新候选 SHA-256：

- EXE：`d9d5dff2fe9bb1c7c9af1f81ada17289d80dba0f2989753a1b3d1d932fc60ef4`
- ZIP：`8f7b25bddbca1fa32ec618291ab476968c9153016e584dfb93b49f5c12a4127f`
- TGZ：`a79abe42607b314f932f32618722528079842b59588e01cabf883936f8ae28f3`（54912376 字节）

新 TGZ 因文档／证据更新而不同于旧 0.8.3 包，旧文件保留。两站已固定带完整提交后缀的新 vendor 文件，更新各自锁文件并完成新包的隔离生产构建、三种数据库各两次迁移、邮件 HTTP 回归、每站 9 项权限检查及双向跨站拒绝。UliUli 已推 `a32e763da57b14e4df49033e789c5ea402fb22db`，Mia 已推 `4085a0a26bfe904e12328db345a885f1e5a74c8c`，原 tsconfig 和旧包保留。两站 PR 均为草稿，未合并、未部署；远端 CI 列表为空，不能声称两站 CI 通过。完整记录见 [两站新包证据](evidence/0.8.3-copy/source-c4efb3d/consumers/logs/report.json)、[推送／PR 状态](evidence/0.8.3-copy/source-c4efb3d/consumers/pull-requests-final.json) 和 [分提交验收说明](evidence/0.8.3-copy/SOURCE-QC.md)。

卸载调查的新对照发现：同一无删除诊断在命令行可读隔离安装记录，Explorer 双击却在最早的注册表安装位置读取处失败，路径检查尚未执行。两者身份摘要、进程位数和 package identity 检查没有解释该差异；仍在定位启动上下文／注册表可见性的原因。因此不能把 c4efb3d 的短路径兼容修复等同于用户弹窗已解决，原生门槛保持未通过。

安装前原生基线显示两个测试安装注册记录不存在，旧测试快捷方式仍在，会触发防止覆盖的 `CaptureLink` 保护。用户随后已亲手删除仅这两个测试 `.lnk`，原应用和旧 6b 临时文件未动。18:04:59 UTC 新原生探针确认 Setup 摘要正确、测试注册记录／快捷方式均不存在、5 项保护文件及原 Squirrel 0.8.0 记录未变，允许新的隔离安装；没有绕过此前自动清理拒绝。

root 已从 Explorer 启动 **c4 新 EXE**，实际走完玻璃安装向导，选择新中文目录并启用两个快捷方式。18:12:19 UTC 原生 V2 探针确认 **75/75 载荷、安装标记、注册信息和快捷方式严格匹配**，原保护文件及 Squirrel 不变。V1 探针此前误读根目录 marker，造成错误失败；改用真实 `resources/windchime-install.ini` 后仍严格核对 AppId／Version。原错误报告保留，这是探针错误，不计作产品故障。详见 [原生安装证据](evidence/0.8.3-copy/source-c4efb3d/native-install/installed-summary-v2.json)。

新安装程序已实际启动，沙箱／上下文隔离及私人窗口检查通过，关闭和重新打开后现留在托盘。[18:12:42 UTC 就绪快照](evidence/0.8.3-copy/source-c4efb3d/native-install/runtime-ready-before-uninstall.json) 明确 `gracefulExit=false`，不代表已正常退出或卸载。root 已请用户亲手启动新目录卸载器，当前尚未启动；运行中卸载提示、正常卸载和升级仍未验收。含私人路径的向导截图未公开。

以下 83d284f／6b53289 的包、采集及界面结果保留为历史检查点，不能将其中“最终”措辞解读为当前 c4efb3d 已完成必要原生验收。

## 此前 83d284f 候选与证据

此前运行源码 47b03b2 的桌面候选因文案修改而作废保留，不用于本轮正式发布。旧证据见 [原候选记录](RELEASE-DESKTOP-083.md)，其中通过项不能代替本轮新包验收。

此前候选源码为 `83d284f2dfa00cc7d9ca94d932717979aff163f6`。截图复查发现 6b53289 还保留两句侧栏文案，因此又删掉“你的创作空间”“每一封，都由你决定”及安装器顶部装饰英文，重新构建桌面包。6b53289 桌面包保留但不再作为发布候选。

共享包在 6b53289 生成，83d284f 的干净工作区重新打包后字节完全一致：`windchime-embed-0.8.3.tgz`，31,415,194 bytes，SHA-256 `c1207a59e8a3d5f9fe9e4e9a6c1895250569f2985add9b08c9737d8170a8f865`。两站固定同一文件。原验证来源与重现来源分别记录，不将旧测试伪记为重新运行。

本轮证据保存在 [evidence/0.8.3-copy](evidence/0.8.3-copy/)。任何影响产物的后续修改均重新打包并重测受影响项。

## 实际结果（2026-09-24）

| 项目 | 结果与范围 |
| --- | --- |
| 共享业务 | 238/238 通过；同一 TGZ 独立安装、8 个入口、SQLite 和严格 TypeScript 消费者检查通过 |
| 桌面回归 | 最终源码 145/145 通过，包括 26 项安装事务与 8 项 Unicode 快捷方式回归 |
| CI | 最终源码 Windows desktop、Ubuntu Node 20/24 均通过：[本次运行](https://github.com/Morphling0717/WindChime/actions/runs/35885412788) |
| 两站 | 隔离生产构建、fresh/legacy/current 各重复迁移两次、原登录后的邮件 HTTP 回归、每站 9 项额外权限检查以及双向跨站拒绝通过 |
| 私人界面 | 最终源码管理回归 16 组、315 次合成请求通过；不同窗口尺寸可选择网站和话题，取消切换保留未保存内容，海报配置在独立进程恢复 |
| 磁贴 | 6b53289 源码 11 组、233 次请求及独立进程重启通过；实际可见的五个磁贴原生置顶状态正确，之后仅删除桌面文案。此处快捷键回调经过测试，未冒充真实按键 |
| 展示布局 | 最终源码的真实 Electron 展示入口通过 5 组检查，覆盖缩短窗口、96px 字号、保持图片比例和位置、后台滚动、清屏后不恢复；不是直播软件采集证明 |
| 最终 ZIP 布局与重启 | 六种排版 × 三套主题共 18 组通过；实际 150% 显示缩放，图片保持比例并固定在滚动正文下方。两个独立进程保留同一合成连接及批准列表，重新打开展示保持空白，直到新的手动上屏；此项为最终程序渲染检查，不代替直播软件采集 |
| 真实快捷键 | 最终 ZIP 的私人窗口中，通过电脑控制工具实际发送 `Ctrl+Alt+PageDown`，服务端当前快照及展示文字均切为第二封；实际发送 `Ctrl+Shift+H` 后当前快照为空，展示窗口无来信内容。没有调用热键回调模拟按键，也没有用工具往返时间冒充采集清屏时间 |
| 安装 | 被替换的 6b53289 包实际中文目录安装、Unicode 快捷方式、注册信息及 EXE/ASAR 摘要检查通过，隔离配置启动和正常退出通过；最终 83d284f 包仍须完整原生安装复验 |
| 最终包 | 83d284f 安装 EXE 与便携 ZIP 内容检查通过，安装引擎隔离解包 75/75 文件匹配；这不是实际安装往返 |
| 最终 ZIP 与 OBS | 从最终 ZIP 解压的程序独立启动，通过与构建载荷一致的 EXE/ASAR 摘要检查；两个独立程序及合成配置分别执行普通 17 组、故障 23 组实际窗口采集，正常退出。批准空白、透明、手动上屏、隐藏、撤销、关闭重开、遮挡 70 秒、断线恢复及渲染器卡死后的空白通过；未开播、录制或启用虚拟摄像头 |
| 最终 ZIP 与直播姬（部分） | 用户已添加合成窗口捕获和棋盘格背景。长信／两图、透明、隐藏、关闭空白、重开空白、新手动上屏自动重新捕获、断线后最终空白及恢复不重播通过；未开播。隐藏从真实 HTTP 响应至空白截图返回的上界为 953ms。第一次静默断线的 3 秒时限无法确认，不能宣称完整采集验收通过 |

OBS 实际采集测得：普通隐藏响应后 146ms 清屏；遮挡窗口的远端隐藏响应后 576ms；网站隐藏请求被延迟时本地清屏 212ms。静默断线从最后一个已确认帧请求的发起时间计算为 2896ms，渲染器卡死为 2877ms，分别满足 1 秒和 3 秒门槛。直播姬必须独立验收，此结果不外推。

直播姬独立检查的 [画布证据与报告](evidence/0.8.3-copy/source83/livehime-capture/livehime-capture-report.json) 仅发布经过逐图确认的预览区域，不公开含私人房间信息的原截图。隐藏请求 630 的真实 HTTP 响应结束时间为 `1790182901985`，已空白截图返回于 `1790182902938`，上界为 953ms；原始记录的 637ms 从测试助手完成命令时计算，不能用作 HTTP 响应耗时。第一次断线截图调用始于最后成功帧请求发起后 2762ms，返回于 3207ms，实际取帧时刻不明；渲染观察在 2879ms 无卡片不能代替直播姬采集。故本次 3 秒采集时限记为 **inconclusive**，不是已证明产品超时，也不是通过。

最终候选桌面文件为 `WindChime-Setup-0.8.3.exe`（119,133,184 bytes，SHA-256 `2eadfa26fc5ef36dad7b1c73141a8ad0e52da8ca77a19c45bb60c716cbab297f`）和 `WindChime-win32-x64-0.8.3.zip`（160,660,617 bytes，SHA-256 `f3b63a79606ce6ebc2797293442a6b7d9460ef9b002b1d545bbda4331365cf9c`）。旧候选未覆盖。

两站提交分别为 UliUli `c0956f7f58ca40246dda5d7853120ed55e6ab917`、Mia `09d08bcaad32ee2223fed143f3c8c1f43fdbe872`。开发分支已推送，草稿 PR 已更新；原工作区 tsconfig.json、node_modules、真实数据库和资源未改。

安装验收脚本曾把 NSIS 的 UTF-16LE 安装标记按 UTF-8 读取，造成“Installed product marker”误报。修正为按 BOM 解码并严格核对 AppId/Version 后，14 项脚本回归及实际安装标记检查通过；这不是安装器故障，也没有通过放宽身份检查规避失败。

## 当前受阻项

- **旧 6b53289 实际卸载被拒绝，尚未完成新包卸载验收：**用户手动启动旧卸载器后出现安装目录无法确认提示。CIM 显示卸载器 17:13:05 UTC 启动，采集测试进程 17:13:38.968 UTC 才正常退出；不能断言启动前已退出。后续诊断发现正常双击环境缺少该测试安装注册记录，路径检查尚未执行，不能把它笼统认定为短路径修复已解决。c4 现在已在正常原生上下文完成新安装和启动，但新卸载器尚未启动；仍不能发布。
- 此前电脑控制工具启动卸载器曾被 `product policy blocks this app` 拒绝，没有改用命令行或其他控制工具绕过；现已获得用户亲手操作的上述真实失败结果，因此不能再把卸载未通过仅归因于工具政策。原有安装的历史清理拒绝同样没有重试，隔离安装目录暂保留。
- 用户已处理直播姬的系统授权弹窗，并手动配置本地测试场景、窗口捕获及棋盘格背景。原先场景按钮受 Windows 权限层级限制的记录保留，未提权、改安全设置或换工具绕过。现在已完成上表部分实际采集检查，仍需确认静默断线的 3 秒清屏时限；未开播。
- 真实按键前的桌面观察曾被 OBS 的 Windows 防火墙授权弹窗遮挡，未向安全弹窗发送任何输入。用户处理后，真实系统按键已单独验证通过；原 OBS 报告的 `hotkeysActuallyPressed: false` 保留，本次按键证据另存。
- c4 的新中文目录实际安装、载荷／注册／快捷方式及启动到托盘已通过；0.8.2 升级保留配置、托盘卸载提示、正常卸载、直播姬 3 秒断线清屏时限及真实休眠恢复仍需完成。必要门槛未全部满足，不正式发布或合并。

## 成品门槛

- 共享库、桌面测试、压缩包独立安装检查、两站隔离构建和重复迁移、真实登录 HTTP 业务与权限回归。
- 窄窗口和高 DPI 下的新文案及玻璃界面；六种排版、主题、字号、固定图片、长文字循环滚动。
- 磁贴置顶、实际系统热键、未保存稿保护、断线重启和休眠恢复空白。
- 最终 EXE 在新隔离中文目录安装、快捷方式、注册信息、旧版本升级及合成配置保留、故障回滚、托盘卸载提示和正常卸载。
- 最终 ZIP 独立启动；实际运行的 EXE 和 ASAR 必须与最终载荷清单匹配。
- OBS 和直播姬分别实际窗口捕获，仅本地预览；正常隐藏响应后 1 秒内清屏，静默断线 3 秒内清屏。私人控制、登录及错误不能进入输出。

以上必要验收尚未全部完成，三个 PR 保持草稿，不提前合并或公开发行。

## 发布与生产环境

验收通过后依次合并 [WindChime #1](https://github.com/Morphling0717/WindChime/pull/1)、[UliUli #1](https://github.com/Morphling0717/Next_UliUli/pull/1)、[Mia #1](https://github.com/Morphling0717/Next_Mia/pull/1)，使用保留历史的合并方式。核对合并树与候选源码一致后，从 WindChime main 创建 v0.8.3 正式草稿。

发布四个资产：安装 EXE、便携 ZIP、`windchime-embed-0.8.3.tgz` 和包含前三者的 SHA256SUMS.txt。核对远端摘要后才公开并设为最新；保留 0.8.2 原标签和文件。

UliUli 部署与合并分开，仍须满足 8 GiB 容量、备份、隔离构建与回退条件；上次只读检查线上为 0.8.1，未达到容量门槛。Mia 本轮不部署。旧安装、个人配置、概念图和两站未提交的 tsconfig.json 保留。签名、自动更新及 B 站官方接入不属于本轮交付。
