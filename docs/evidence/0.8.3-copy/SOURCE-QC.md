# 0.8.3 文案修订：源码与界面复核

本目录记录源码测试、隔离 Electron 联调、最终 ZIP 的实际 OBS 窗口采集和真实系统热键操作。**最终安装包的安装／升级／卸载及直播姬采集仍未全部完成**，不能据此认定所有发布门槛已通过。

最终桌面源码为 `83d284f2dfa00cc7d9ca94d932717979aff163f6`，tree 为 `6e569f3c770feecf824cd668b6898f468d73c16d`。桌面和共享库版本均为 0.8.3。`source-83d284f/` 记录这次提交的测试；`checkpoint-6b53289/` 是此前 `6b53289c6f0a513d8789d099dc882c13119142b5` 检查点，不能将它的旧界面截图当作最终桌面截图。

## 本次实际结果

| 验证 | 结果与范围 |
| --- | --- |
| 共享库本地全量测试 | 检查点 6b53289：238/238，通过；最终提交仅修改 5 个桌面文件，共享源码与精确 TGZ 均未改变。 |
| 桌面本地全量测试 | 最终提交 83d284f：145/145，通过；Node 22.22.3、npm 10.9.8、Windows x64。包含构建与 Windows 安装器回归，但不代替最终 EXE 的人工安装。 |
| 桌面管理真实服务联调 | 最终提交：16 项、315 次请求，通过；真实 SQLite/HTTP、隔离合成信件，覆盖密钥导入、话题同步与选择、读取/收藏/CSV、分享保存、关键词设置、授权列表同步、脏稿保护及外观预览。第二个独立 Electron 进程验证海报设置恢复与实际 PNG 保存；不会恢复展示窗口。 |
| 展示窗口尺寸联调 | 最终提交：5 项、49 帧、2 次接收连接，通过；固定图片、文字滚动、短窗口、高字号与重新打开空白。使用合成只读 IPC 帧，**不含 OBS 或直播姬采集**。 |
| 磁贴与下一封逻辑 | 检查点 6b53289：11 项、233 次请求，通过；5 个磁贴就绪后原生置顶均为 true；包含权限限制、未保存稿、冲突、批准不播放、上屏/撤销/下一封、切换隔离及第二独立进程重启。快捷键注册经测试拦截，调用的是实际回调；**未发送操作系统按键**。 |
| GitHub CI | 最终提交 push `35885408854`、PR `35885412788` 均 completed/success；各自 `verify (20)`、`verify (24)`、`desktop-windows` 均成功。Node 24 Docker 构建成功；Node 20 的 Docker 步骤按工作流条件跳过。旧检查点两次 CI 另存作来源记录。 |

主要命令：共享库 `npm test`、`npm run pack:check`；桌面目录 `npm test`，以及 `electron.exe scripts/management-smoke.cjs`、`electron.exe scripts/tiles-smoke.cjs`、`electron.exe scripts/display-viewport-smoke.cjs`。各脚本使用隔离测试配置和合成数据。CI JSON 包含逐步结果和时间。

## 文案与截图复核

6b53289 初次实际截图仍有“你的创作空间”“每一封，都由你决定”两句侧栏文案；它们和多余的安装器英文装饰标题已在 83d284f 删除。Logo、品牌名、操作名称、防误播说明、连接失败及保存冲突提示保留。未改写用户来信、昵称或数据。

实际查看最终截图 `inbox-1200x620`、`studio-961x700`、`settings-wide`、`appearance-mia-narrow`、`appearance-uliuli-narrow`，以及两张展示尺寸截图。上述口号已消失；侧栏无残留空段，网站与话题选择、隐藏按钮没有遮挡，窄窗口的主题选项与预览可读。预览昵称、正文和图片说明为中性示例。主题名称保留在私人设置中，观众内容未添加主题名称。

管理截图中的 `Ctrl+Shift+H 快捷键注册失败` 是本轮另一份隔离安装测试程序仍在托盘运行造成的占用，属于私人控制台错误提示；它没有进入展示内容。不能据此声称实际系统热键已经通过。海报中的合成标题、署名为测试输入，不是产品默认口号。

## 精确共享包与源码对应

共享包 `windchime-embed-0.8.3.tgz`：31,415,194 字节，SHA-256 为 `c1207a59e8a3d5f9fe9e4e9a6c1895250569f2985add9b08c9737d8170a8f865`。原始安装、类型和 SQLite 验证报告 `shared-package-verification.json` 仍明确归属 6b53289，未改写或替换。

在干净、已提交的 83d284f 隔离工作树重新执行 npm pack，得到**逐字节相同**的 TGZ；详见 `shared-package-reproduction-83d284f.json`。这证明新桌面源码仍能生成网站已固定的同一共享包，并不声称原先的共享包验证是在新提交上重跑。两站的验收由独立 `consumers-report.json` 记录。

最终候选的后台来源记录位于 `source-83d284f/candidate/`。候选目录已具有四份发布资产、三载荷校验清单、桌面包独立验证报告、原始共享包验证报告及重新打包证明。来源协议为 schema 2、状态 `built-pending-native`；发布校验器仍按预期拒绝发布准备。后台便携包预检仅核对包内字节与适配器语法，`startedAnyApp=false`，不代表最终程序已经启动。

候选共享包报告的 `sourceCommit` 记录这次可复现来源 83d284f，`verificationSourceCommit` 保留真实执行安装、类型和 SQLite 验证的 6b53289；原始报告与重新打包证明都有独立摘要引用。公开副本经过文本规范化，来源协议内报告摘要仍指向候选目录的原始字节，不能拿公开副本直接替换本地发布报告。

## 最终便携包的 OBS 实际采集

2026-09-23 16:09–16:11 UTC 的两个独立运行已归档到 `source-83d284f/packaged-obs/full/` 和 `faults/`。两次均启动最终 ZIP 解压出的 WindChime.exe，使用独立合成配置、真实管理桥接导入、实际 OBS 32.0.1 窗口捕获和 OBS WebSocket 5.6.3；并未使用源码开发版代替成品。程序 EXE、app.asar、ZIP 及构建信息的摘要全部与最终候选预检一致。

- ZIP SHA-256：`f3b63a79606ce6ebc2797293442a6b7d9460ef9b002b1d545bbda4331365cf9c`
- 运行程序 EXE SHA-256：`9fe566330366bfd92cd24f74e34ab66914c6fa359d014f6917a0551352507987`
- app.asar SHA-256：`528a092fbe10d5ea5f0dae7bea759b5967092a4ea243e9463e66fa2e964264ff`

完整采集 17 项通过，故障采集 23 项通过。验证未审核空白、仅批准仍空白、手动上屏、透明背景、隐藏、撤销、关闭和重新打开；输出关闭后没有采到私人控制台。完整采集还实际覆盖遮挡展示窗口 70,031 毫秒后继续控制。故障运行包含静默断线、渲染进程冻结及恢复后保持空白。两次均未开播、录像或启动其他输出，B 站及网关请求为 0；测试授权撤销，WindChime 程序正常退出。主验收线程报告 OBS 清理仅针对测试脚本自己启动的子进程。

| 实际采集计时 | 完整运行 | 故障运行 | 门槛 |
| --- | ---: | ---: | --- |
| 隐藏响应后清屏 | 146 ms | 136 ms | ≤ 1,000 ms |
| 远端隐藏响应后，遮挡窗口清屏 | 576 ms | 572 ms | ≤ 1,000 ms |
| 网站响应延迟时，本地紧急隐藏命令至清屏 | 212 ms | 216 ms | 不等待网站响应 |
| 静默断线：最后确认状态对应帧发出至实际采集空白 | — | 2,896 ms | ≤ 3,000 ms |
| 渲染冻结：最后状态帧发出至实际采集空白 | — | 2,877 ms | ≤ 3,000 ms |

已实际查看上屏源、透明合成、静默断线空白和冻结清屏 PNG；画面仅有合成审阅内容及其图片，无私人控制台、通知或错误详情。报告中的重复截图名称代表连续采样观察，归档 PNG 是该名称最终保存的一帧；计时来自原报告，不能把一张静态图当作时序证明。

交互测试记录明确为 `hotkeysActuallyPressed=false`。当时电脑操作工具看到 Windows 安全中心的 OBS 防火墙对话框，未操作安全对话框、也未发送系统热键；不能把回调测试或快捷键配置当作实际按键通过。此次没有直播姬采集通过记录。

## 最终程序的主题、排版与 150% DPI

`source83/display-matrix/` 归档的是安装验收线程已完成的最终程序渲染测试，整理时没有重跑测试。源码仍为 83d284f；报告内 EXE、ZIP、运行程序和 app.asar 摘要与前述最终候选一致。2026-09-23 16:23:36–16:24:00 UTC，两次独立打包程序进程使用隔离配置，实际显示缩放均为 **150%**，并非将源码浏览器截图作为成品截图。

18 个组合全部通过：`stack`、`split`、`banner`、`sidebar`、`portrait`、`focus` 六种排版，分别搭配 `pure`、`uliuli`、`mia` 三种主题。测试字号为 26 或 32 px。各组合的正文均实际发生滚动、图片位置保持固定，横向无溢出，展示窗口没有私人控件或私人提示。两幅合成图片分别为 16:9 和 2:3，按原图比例显示。

另外 10 项检查通过，包括新投稿和批准后保持空白、独立第二进程仍保留批准队列和连接、重开输出不恢复旧信、再次手动上屏正常。两次进程仅访问合成站点；未使用生产数据，未改变网络权限，测试授权已撤销。

归档包含完整报告、18 张组合截图、隐藏与重启空白截图以及总览图 `contact-sheet.png`。实际查看总览可见横向、双栏、侧栏和纵向排版分开呈现，图片未跟正文一起滚动的结论来自报告的前后几何检查。总览上方的英文组合名是测试图注，不属于观众展示 UI；合成正文也不是产品默认文案。

本项是原生展示窗口渲染检查，明确保留 `nativeCaptureValidated=false`、`obsStarted=false` 和 `realKeysPressed=false`，不重复计作 OBS／直播姬采集或系统热键通过。随后单独完成的真实热键操作见下一节。

## 单独的真实系统热键操作

`source83/system-hotkeys/` 包含一份明确归属主验收线程工具观察的报告，以及两份原始合成站点状态快照。程序仍对应最终 83d284f 候选，资产和运行文件摘要已复核。这次主验收线程实际使用 `sky.press_key`，目标为 WindChime 私人控制窗口；没有通过脚本回调冒充键盘输入。

- 2026-09-23 16:35:27.274 UTC 发送 `Control_L+Alt_L+Next`（配置为 Ctrl+Alt+PageDown）。主验收随后读取原生展示窗口文字为“合成投稿人二／第二封合成来信 · 真实快捷键验收”，站点当前信件变为第二封 `35261d84-22a9-42b6-b7b0-5a5a561ac4f2`。
- 16:35:52.883 UTC 发送 `Control_L+Shift_L+h`。当前信件和快照均变为 null；主验收读取原生展示窗口，确认没有来信文字。

两项实际按键检查通过。报告将“主验收线程发送按键和读取原生窗口的观察”与“本次整理时核对的已保存站点状态”分开注明。保存快照的更新时间分别为 16:35:41.582 和 16:36:04.244 UTC，晚于主验收最初观察时间；这些文件证明结果状态，不用于推算毫秒级响应速度。

本次电脑操作截图异常带出了不相关的 Codex 背景，因此未公开、未用作证据；仅使用按键工具观察、只读窗口文字检查和合成状态。此验收目标是实际私人窗口，不额外宣称在其他全屏游戏持有焦点时也已验证。此前 OBS full/faults 和渲染矩阵报告的 `hotkeysActuallyPressed=false`／`realKeysPressed=false` 均保持原值，因为那些运行没有发送系统按键。

## 尚未完成的原生验收

初次原生操作时，卸载程序的电脑操作被工具返回 `product policy blocks this app` 阻止，直播姬也曾停在 Windows UAC；没有改用其他工具绕过。**随后用户已处理 OBS 与直播姬安全弹窗**，因此 UAC 不再作为当前未处理项。直播姬一度退出未显示主界面；最新操作通过 Explorer 打开现有开始菜单的正常快捷方式后，**直播姬 7.54.0.10521 主窗口与预览界面已成功出现，未开播**。

当前直播姬交互受 Windows 权限层级限制：电脑操作工具的 `get_window_state` 明确返回 `Accessibility is limited because the target window has higher Windows integrity than the Computer Use helper.`。主验收按最新截图尝试点击场景添加控件两次，未获得响应；没有提权、改变安全设置或换其他控制工具绕过。现已请求用户手动创建空白测试场景，并添加 `WindChime Display · synthetic render 95a1991b` 窗口捕获。**目前仍无直播姬采集通过证明**，不能用成功启动代替采集验收。

上述最新工具观察独立记录于 `source83/livehime-observation/interaction-limited.json`，明确由主验收线程提供；没有公开包含私人房间标题的全界面截图，旧证据内容未改。用户尚未回应手动卸载，卸载验收仍未完成。

最终候选 EXE 的安装、升级、卸载及直播姬窗口捕获仍由主验收流程负责。最终 ZIP 的 OBS 采集与上述范围内真实系统热键已单独通过，不能代替其他门槛，不据此设置 `final-reviewed`、正式发布或合并。

生产检查只读：主验收线程报告剩余 8,293,748,736 字节，小于既定 8 GiB 门槛，运行中站点健康检查为 200。本次没有部署或清理生产数据；服务器地址与个人主机信息不写入公开证据。

## 文件完整性与脱敏

另补充旧检查点的实际首次安装记录到 `checkpoint-6b53289/native-first-install/`，仅包含已明确可公开的安装前／安装后保护核对、安装后运行验证三份 JSON，原始字节未改。归属源码 **6b53289**，对应旧安装 EXE 为 119,133,696 字节，SHA-256 `8ed71daf4ae8b386780b3a58f3219140934633abc42c004d0a9f05237c429ad8`。三份报告记录原有文件和配置未变、隔离目标注册信息及快捷方式符合预期，安装后的私人窗口、Logo、沙箱、托盘关闭重开及正常退出通过；未自动恢复展示。该候选随后被文案补漏提交 83d284f 取代，**不计作最终 83d284f 安装、升级或原生卸载通过**。未复制 baseline、active-runtime、配置、nonce、私人验证资料或包含桌面背景的电脑操作截图。来源及旧 EXE 摘要另外写入索引的 `supersededNativeFirstInstall`。

`source-qc-manifest.json` 列出收集时间、来源提交、输入文件和原始 SHA-256，以及公开副本的字节数与 SHA-256。源码联调文本替换个人目录、JSON 重排为 LF，因此两种摘要分开记录。OBS 的两份 `final-binary-verification.json` 和全部 PNG 保持原始字节；OBS `report.json` 仅移除临时站点地址并规范 JSON。局部 `.gitattributes` 设置 `-text`，防止 Git 自动换行导致证据摘要失效。没有收集授权、端口、OBS 配置、交互命令文件或旧运行失败截图，也没有覆盖共享包和两站验证线程提供的文件。
